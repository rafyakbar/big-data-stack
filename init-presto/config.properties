coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080

# --- Memory Configuration (Trino >= v380) ---
# Total memory untuk query (disarankan ≤ 80% dari RAM container)
query.max-memory=4GB

# Memory per query per node (termasuk coordinator jika include-coordinator=true)
query.max-memory-per-node=2GB

# --- Discovery ---
discovery.uri=http://presto:8080